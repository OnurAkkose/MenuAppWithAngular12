export interface MenuThema {
  bodyColor: string;
  bottomColor: string;
  extraImage: string;
  id: number;
  itemColor: string;
  message: string;
  ownerName: string;
  placeName: string;
  placePhoto: string;
  productId: number;
  textColor: string;
  topColor: string;
}